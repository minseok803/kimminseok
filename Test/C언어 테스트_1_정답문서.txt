C언어 테스트

1. 변수란 무엇인가?
 - 하나의 데이터를 저장 할 수 있는 메모리 공간

2. %d, %f, %s... 등 이와같은 것들을 뭐라고 부르는가?
 - 출력타입, 서식문자

3. 숫자앞에 '0x'(영엑스) 라고 적혀있는 것은 무슨 의미인가?
 - 16비트 표현

4. if의 종속문장(if의 조건식이 참일때 수행하는 문장)은 일반적으로
{}(중괄호) 로 묶어준다. 이 중괄호는 어떠할 때 생략가능한가?
 - 다중열의 코딩이 만들어지지 않을때만 가능하다.

5. switch문을 사용할 때 switch(  )에서 괄호안에는 무엇을 넣는가?
 - 조건, 변수

6. && 연산자의 의미는?
 - 논리연산자 앤드조건을 의미한다. con1 ? con2 : false이다.

7. 
int a=5,b=0;

if(a>4)
	b++;
else if(a==5)
	b++;
else if(a>1)
	b++
else
	b--;
printf("%d", b);

위의 소스에서 마지막 출력되는 b의 값은?

1


8.
int sum(int x, int y)
{
	int result;
	result=x+y;
	return result;
}	
void main()
{
	printf("%d",sum(3,4));
}

위의 소스에서 각 부분을 지칭하는 용어를 작성하시오.
①int sum 에서 'int'가 있는 부분 : 반환형
②int x,int y 가 있는 부분 : 매개변수
③sum(3,4) : 함수 호출
④3,4가 있는 부분 : 인자

9. int arr[12]라는 배열이 존재할 때
	arr의 요소는 arr[?] ~ arr[?] 까지 존재한다
'?' 안에 들어가는 각 숫자는?
0에서 11까지

10. 배열명은 배열의 '????' 이다
포인터

11. int a = 10;
    int *p = &a;
    int **pp = &p;
    int ***ppp = &pp;

다음 소스를 그림으로 표현하시오(그림을 첨부)



12. 포인터 변수는 ?byte 이다
4바이트


13. 
int arr[5]={10,20,30,40,50}
int *p=arr;
일 때 *(p+3) 의 값은? 그리고 *(p+3)을 다르게 표현해 보시오
40, arr[3]


14.
struct people
{
	char name[20];
	int age;
};

void main()
{
	struct people p1={홍길동, 15};
}

위의 소스에 있는 p1이라는 구조체 변수의 멤버변수 age에 20을 넣으려고 한다
어떠한 코드를 작성해야 할까?

p1.age = 20;

15. 구조체 포인터를 이용하여 멤버변수에 접근하려고 할때 사용하는 연산자는?
->

16. 실행된 프로그램은 메모리상에 네가지 영역으로 나뉘어서 올라간다
	그 네가지 영역을 적고 어떤것이 속하는지 기입하시오.
코드영역 - 실행할 프로그램의 코드
데이터 영역 - 전역 변수, 정적 변수
스택 영역 - 지역 변수, 매개 변수
힙 영역 - 동적으로 할당되어진 것들.

17. 동적할당을 사용하기 위해서 malloc()함수를,
동적할당한 공간을 해제하기 위해서 free() 함수를 사용한다
두 함수가 정의 되어있는 헤더파일은?
stdlib.h

18. "malloc 함수를 호출하면 인자값만큼의 공간을 할당하고 그 공간의
시작주소를 반환한다. 하지만 반환되는 주소는 void* 형이기 때문에
(    )을 해주어야 한다." 괄호안에 들어갈 단어는?
형변환 
